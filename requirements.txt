# Warden — Zero-Trust Security Runtime for AI Agents
# The runtime is deliberately stdlib + PyYAML only. For development, prefer
# the exact pins in requirements.lock (what CI and releases are tested
# against); this file carries the loose set.
pyyaml
pytest
hypothesis
# Optional capability, opt-in via extras (see pyproject.toml):
#   warden-security[pii]      presidio-analyzer  (v1.5.5 richer PII)
#   warden-security[sign]     cryptography       (v7 signed policy bundles)
#   warden-security[desktop]  pywebview          (v7 native dashboard window)
